# Component Inventory - Main Project

## Overview

This document provides an inventory of custom components identified within the `odoo-env` project.

## Custom Odoo Components

Based on the scan of the `.devcontainer/addons` directory, no custom Odoo modules or components were identified. The project currently relies on Odoo's core functionalities and installed third-party modules (if any, defined in the Docker environment).

## Other Components

*   **Docker Containers**:
    *   Odoo Application Container
    *   PostgreSQL Database Container
*   **Configuration Files**:
    *   `odoo.conf`
    *   `docker-compose.yml`
    *   `Dockerfile`
*   **CI/CD Configuration**:
    *   `dependabot.yml`